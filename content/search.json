{
  "entries": [
    <% @items.find_all('/*/*/*.{asciidoc,md}').reject { |item| item[:exclude_from_search] }.each do |item| %>
      <% if item[:title] %>
        <% @clean_body = compress(safe_embed(item.compiled_content)) %>
        {
          "title": "<%= safe_embed(item[:title]) %>",
          "category": "",
          "category_url": "",
          "url": "<%= item[:filename].sub(/^content/, '').sub(/.(asciidoc|md)$/, '/') %>",
          "body": "<%= @clean_body %>",
          "excerpt": "<%= @clean_body[0..140] %>…"
        },
      <% end %>
    <% end %>

    <% latest_atom_version_apis.each do |klass| %>
      <% body = compress(safe_embed(klass['description'])) %>
      <% root_url = "/api/v#{latest_atom_version_number}/#{klass['name']}" %>

      {
        "title": "<%= klass['name'] %>",
        "category": "",
        "category_url": "",
        "url": "<%= root_url %>",
        "body": "<%= body %>",
        "excerpt": "<%= body[0..140] %>..."
      },

      <% klass.fetch('classMethods', []).each do |class_method| %>
        <% body = compress(safe_embed(class_method['description'])) %>
        {
          "title": "<%= klass['name'] %>::<%= class_method['name'] %>",
          "category": "",
          "category_url": "",
          "url": "<%= root_url %>/#<%= class_method['name'] %>",
          "body": "<%= body %>",
          "excerpt": "<%= body[0..140] %>..."
        },
      <% end %>

      <% instance_members = klass.fetch('instanceProperties', []) + klass.fetch('instanceMethods', []) %>
      <% instance_members.each do |member| %>
        <% body = compress(safe_embed(member['description'])) %>
        {
          "title": "<%= klass['name'] %>::<%= member['name'] %>",
          "category": "",
          "category_url": "",
          "url": "<%= root_url %>/#instance-<%= member['name'] %>",
          "body": "<%= body %>",
          "excerpt": "<%= body[0..140] %>..."
        },
      <% end %>
    <% end %>

    {}
  ]
}
